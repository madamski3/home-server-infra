services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    restart: unless-stopped
    ports:
      - '${TAILSCALE_IP}:3001:3001'
    volumes:
      - ./data:/app/data
    networks:
      - server-network

networks:
  server-network:
    external: true
